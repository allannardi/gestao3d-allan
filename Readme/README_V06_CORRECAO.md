# Correção Mobile v01

Corrigido erro de sintaxe causado por aspas escapadas nas chamadas:

mobile_bottom_nav("dashboard")
mobile_bottom_nav("pedidos")
mobile_bottom_nav("pecas")
mobile_bottom_nav("clientes")
mobile_bottom_nav("mais")

Substitua os arquivos da versão anterior por esta versão corrigida.
