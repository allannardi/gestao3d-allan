# Gestão 3D Allan v06 - Mobile v09 menu profissional

Alterações:

- Mantém `st.segmented_control` + `st.switch_page` para preservar o login.
- Remove o visual de tabela do menu inferior.
- Remove bordas duras entre os itens.
- Usa fundo translúcido com blur, sombra e item ativo em formato de pill.
- Novos READMEs ficam dentro da pasta `Readme/`.

Próximo passo sugerido:
- Caso desejado, criar `assets/icons/` com SVGs próprios para cada aba.
