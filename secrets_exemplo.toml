[auth]
username = "allan"
password = "TROCAR_ESSA_SENHA"

[database]
# Para rodar local, use:
mode = "local"

# Opcional: define um arquivo SQLite separado para esta empresa/base.
# Se não informar, o padrão continua sendo database/atelie.db.
# local_path = "database/empresa_teste.db"

# Para rodar na nuvem com Turso/libSQL, cada empresa deve usar seu próprio banco/url/token:
# mode = "turso"
# url = "libsql://SEU-BANCO-DA-EMPRESA.turso.io"
# auth_token = "SEU_TOKEN_TURSO_DA_EMPRESA"
